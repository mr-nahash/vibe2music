# Core pipeline
anthropic
librosa
pyloudnorm
soundfile
numpy
pyyaml
Pillow

# Web control panel backend
fastapi
uvicorn[standard]
python-multipart

# YouTube upload
google-api-python-client
google-auth-oauthlib

# Install the CUDA-enabled torch wheel and ACE-Step separately with
# setup_local.sh or runpod/setup.sh. Keeping them out of this file avoids
# silently installing a CPU-only build on a GPU machine.
